# EV-Charger-Reservation-System
##  Supabase(PostgreSQL) using SQLAlchemy from Python feat. FastAPI

### Version 1.1.0: Last Update
This is last update in 'supabase' branch.
(We discuss about what database we're using . So FinaleNova just doing Supabase for example.?)
Because, we're changing from using 'FULL' SQLAlchemy to Raw SQL instead(But still using SQLAlchemy).
Otherwise, it'll be full of Python and didn't using any Raw SQL we learned(SQLite).
Just so it match the objective of the project.
OF COURSE, WE WOULDN'T DO IT IF IT ISN'T NECCESSARY.
I(FinaleNova) WOULD STICK TO SQLALCHEMY.
#### PS. This is stupid yap. Just know that this is old version of this project.